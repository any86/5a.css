@charset "UTF-8";
/**
* 溢出省略号
* @param {Number} 行数
*/
/**
* 等边三角形
* @param {String} 尺寸
* @param {Color} 颜色
* @param {String} 方向
*/
/**
* 真.1px边框
* {List}: 多个方向边框, 默认值为bottom, 你可以根据需要传入(top, left, bottom, right) 4个方向;
* {Color} 边框的颜色, 默认#ccc;
* {List} 4个圆角半径, 默认0;
* {String} 一个高级设置, 一般都不需要改动, 由于细边框的实现使用了css的伪类, 所以为了规避可能出现的样式冲突, 我们可以自己指定使用:after还是:before, 默认after;
*/
/**
* 棋盘背景
* @param {Color} 背景色
*/
.a-picker-view {
  overflow: hidden;
  position: relative;
  display: flex;
  background-color: #fff; }
  .a-picker-view__content {
    flex: 1; }
    .a-picker-view__content__item {
      padding-right: 8px;
      padding-left: 8px;
      font-size: 16px;
      text-align: center;
      height: 44px;
      line-height: 44px; }
  .a-picker-view__mask {
    position: absolute;
    top: 0;
    right: 0;
    left: 0;
    bottom: 0;
    background-size: 100% 44px;
    background-image: linear-gradient(180deg, rgba(255, 255, 255, 0.9), rgba(255, 255, 255, 0.4)), linear-gradient(0deg, rgba(255, 255, 255, 0.9), rgba(255, 255, 255, 0.4));
    background-repeat: no-repeat;
    background-position: top, bottom;
    backface-visibility: hidden;
    pointer-events: none; }
  .a-picker-view__indicator {
    position: absolute !important;
    width: 100%;
    border-top: 1px solid #ccc;
    border-bottom: 1px solid #ccc;
    border-radius: 0 0 0 0;
    top: 0;
    bottom: 0;
    margin: auto;
    height: 44px; }
    @media only screen and (-webkit-min-device-pixel-ratio: 2) {
      .a-picker-view__indicator {
        position: relative;
        border-top: none;
        border-bottom: none; }
      .a-picker-view__indicator:after {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        display: block;
        width: 200%;
        height: 200%;
        transform: scale(0.5);
        box-sizing: border-box;
        padding: 1px;
        transform-origin: 0 0;
        pointer-events: none;
        border: 0 solid #ccc;
        border-top-width: 1px;
        border-bottom-width: 1px;
        border-radius: 0 0 0 0; } }
    @media only screen and (-webkit-min-device-pixel-ratio: 3) {
      .a-picker-view__indicator:after {
        border-radius: 0 0 0 0;
        width: 300%;
        height: 300%;
        transform: scale(0.3333); } }
